pluginManagement {
    repositories {
        maven("https://maven.fabricmc.net")
        maven("https://maven.meteordev.org/snapshots")
        gradlePluginPortal()
    }
}

rootProject.name = "sb-additions"
